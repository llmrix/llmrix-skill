[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llmrix-skill"
version = "0.2.9"
description = "A professional Git-based Skill management library for LLM agents."
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
authors = [
    {name = "llmrix", email = "support@llmrix.com"}
]
dependencies = [
    "pyyaml>=6.0",
    "filelock>=3.0.0",
    "sqlalchemy>=2.0.0",
]

[project.urls]
Homepage = "https://github.com/llmrix/llmrix-skill"

[tool.setuptools.packages.find]
where = ["."]
include = ["llmrix*"]
